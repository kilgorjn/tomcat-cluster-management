cluster_id: cluster-1
app_id: app-a
nodes:
  - node-1
policy:
  mode: MANUAL
  min_instances: 1
  max_instances: 4
deployment:
  graceful_stop_timeout: 30
  startup_timeout: 60
  health_check_endpoint: /health
